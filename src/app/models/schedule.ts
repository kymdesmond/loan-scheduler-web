export interface Schedule {
    beginningBalance: number
    dueDate: Date
    endingBalance: number
    insallmentAmount: number
    instalmentNumber: number
    interest: number
    principal: number
    scheduledPayment: number
}